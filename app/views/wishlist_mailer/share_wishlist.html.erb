<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      h1 { color: #2c3e50; }
      .gift-item { background: #f8f9fa; padding: 15px; margin: 10px 0; border-radius: 5px; }
      .gift-name { font-weight: bold; color: #2c3e50; }
      .price { color: #27ae60; font-weight: bold; }
      .link { color: #3498db; }
      .footer { margin-top: 30px; padding-top: 20px; border-top: 1px solid #ddd; color: #666; font-size: 12px; }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>🎁 Liste de cadeaux de <%= @user.full_name %></h1>
      
      <p>Bonjour,</p>
      
      <p><%= @user.full_name %> a partagé sa liste de cadeaux avec vous !</p>
      
      <h2><%= @wishlist.title %> (<%= @wishlist.year %>)</h2>
      
      <% if @wishlist.description.present? %>
        <p><em><%= @wishlist.description %></em></p>
      <% end %>
      
      <h3>Cadeaux (<%= @gifts.count %>)</h3>
      
      <% @gifts.each do |gift| %>
        <div class="gift-item">
          <div class="gift-name"><%= gift.name %></div>
          
          <% if gift.price.present? %>
            <div class="price">Prix : <%= number_to_currency(gift.price, unit: "€", separator: ",", delimiter: " ") %></div>
          <% end %>
          
          <% if gift.link.present? %>
            <div class="link">
              <a href="<%= gift.link %>" target="_blank">Voir le produit</a>
            </div>
          <% end %>
          
          <% if gift.reserved? %>
            <div style="color: #e74c3c;">✓ Déjà réservé par <%= gift.reserved_by.full_name %></div>
          <% else %>
            <div style="color: #27ae60;">✓ Disponible</div>
          <% end %>
        </div>
      <% end %>
      
      <div class="footer">
        <p>Ce message a été envoyé depuis Leon Wishlist - Votre application de listes de cadeaux.</p>
      </div>
    </div>
  </body>
</html>
