<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Leon's Wishlist" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Leon's Wishlist">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body>
    <header>
      <nav style="background: #333; color: white; padding: 1rem;">
        <div style="max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center;">
          <h1 style="margin: 0;"><%= link_to "🎄 Leon's Christmas Wishlist", root_path, style: "color: white; text-decoration: none;" %></h1>
          <div>
            <% if user_signed_in? %>
              <%= link_to "My Gifts", gifts_path, style: "color: white; margin-right: 1rem;" %>
              <%= link_to "Groups", groups_path, style: "color: white; margin-right: 1rem;" %>
              <%= link_to "Import Gifts", import_gifts_path, style: "color: white; margin-right: 1rem;" %>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, style: "background: #dc3545; color: white; border: none; padding: 0.5rem 1rem; cursor: pointer; border-radius: 4px;" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path, style: "color: white; margin-right: 1rem;" %>
              <%= link_to "Sign Up", new_user_registration_path, style: "color: white;" %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <main style="max-width: 1200px; margin: 2rem auto; padding: 0 1rem;">
      <% if notice %>
        <div style="background: #d4edda; color: #155724; padding: 1rem; margin-bottom: 1rem; border: 1px solid #c3e6cb; border-radius: 4px;">
          <%= notice %>
        </div>
      <% end %>
      
      <% if alert %>
        <div style="background: #f8d7da; color: #721c24; padding: 1rem; margin-bottom: 1rem; border: 1px solid #f5c6cb; border-radius: 4px;">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
